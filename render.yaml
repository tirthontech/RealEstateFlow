services:
  - type: web
    name: realestate-flow-api
    runtime: node
    buildCommand: npx pnpm@10 install --frozen-lockfile && cd artifacts/api-server && npx pnpm@10 run build
    startCommand: node --enable-source-maps artifacts/api-server/dist/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
